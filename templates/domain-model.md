# Domain Model — {{project_name}}

*Generated by `/speckit.brownkit.discover` (D7). Consolidated, self-contained view.*

## Capability Catalog

<!--
Repeat the block below per L1 capability.

BC-{NNN}: {Capability Name}                           {L2 count} L2s
─────────────────────────────────────────────────────────────────
{2-3 sentence description}

L2 Operations:
  BC-{NNN}-01: {L2 Name}
    Code: {package/module path}
    Entities: {OWNS EntityA, CREATES EntityB}
    Operations:
      - {description} ({HTTP method} {endpoint})
    External: {third-party service} ({purpose})

Security Context:
  Data Sensitivity: ...
  Auth Required: ...
  Exposure: public | internal
  Criticality: low | medium | high

QA Context:
  Coverage: unit {x%} · integration {x%} · e2e {x%}   [source: jacoco | proxy | not-collected]
  Automation: regression {...} · smoke {...} · contract {...}
  Testability: good | impeded | blocked  ({N findings})
  Defect Profile: {N} open · {N} flaky · velocity {high|med|low}
  Environments: covers {...} · missing {...}
  Strategy Gaps: ...

Cross-Capability Dependencies:
  → BC-{NNN} {Capability Name} ({what is shared})
-->

## Entity Catalog

## Ownership Matrix

## Dependency Graph

## Bounded Context Candidates

## Infrastructure Classification
